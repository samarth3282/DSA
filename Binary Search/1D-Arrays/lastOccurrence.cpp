#include <bits/stdc++.h>
using namespace std;
int lastOccurrence(vector<int> &arr, int target){
    int low = 0;
    int high = arr.size();
    while (low<=high)
    {
        int mid = ((low + high) / 2);
        
    }
    
}
int main()
{
    
    return 0;
}